{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for phonebook",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo \"No build step required\"",
    "build:ui": "rm -rf dist && cd /d/dev/FullStackOpen/FullStackOpen-Submissions/part2/phoneinfo && npm run build && cp -r dist ../../phoneBook/backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
